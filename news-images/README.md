# News images

Drop article thumbnails or screenshots of leader posts here, then
reference the filename in `js/news-data.js` (the `image` field).

Keep them web-sized (~1000px wide, under ~300 KB) for fast loading.
